version: '3'
services:
  navigation-dev:
    build:
      context: .
    image: navigation-summer
    container_name: navigation-2025
    stdin_open: true
    tty: true
    privileged: true


    network_mode: host
    working_dir: /home/user/Eurobot2024-Navigation-ws

    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      # Mount X11 server
      - /tmp/.X11-unix:/tmp/.X11-unix
      - /dev:/dev
      - $HOME/.Xauthority:/home/user/.Xauthority
      - D:\DIT\eurobot_navigation\eurobot_nav_ws:/home/user/Eurobot2024-Navigation-ws

    environment:
      - DISPLAY=172.30.176.1:0.0
      - XAUTHORITY=/home/user/.Xauthority

    command: /bin/bash